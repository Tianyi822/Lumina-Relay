# lumina-relay 配置文件
# 环境变量（LUMINA_* 前缀）优先级高于此文件。

log:
  level: info          # debug / info / warn / error
  file:
    enabled: true
    path: logs/lumina-relay.log
    maxSizeMB: 100
    maxBackups: 7
    maxAgeDays: 28
    compress: true

server:
  port: 8443
  host: 0.0.0.0
